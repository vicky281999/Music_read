<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="spider.task_4">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/images"
        android:label="@string/app_name"
        android:roundIcon="@drawable/images"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Top_Tracks_List">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Top_Artist_List" />
        <activity android:name=".Search_Tracks" />
        <activity android:name=".Search_Artists" />
        <activity android:name=".TrackDetails" />
        <activity android:name=".AuthurDetails" />
        <activity
            android:name=".SearchTrackDetails"
            android:label="@string/title_activity_search_track_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SearchArtistDetails"></activity>
    </application>

</manifest>